# Tests

This directory serves as the root for project-wide tests.

Currently, most tests are located within the specific app directories to keep them close to the code they test:

- **Cell Cover App Tests**: `image_gen_mcp/apps/cell_cover/tests/`

## Running Tests

To run all tests in the project:

```bash
uv run pytest
```
