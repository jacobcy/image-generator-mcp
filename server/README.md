# HTTP Server Interface

This directory contains the implementation of the HTTP REST API server, which wraps the `image_gen_mcp` core library.

## Components

- **`main.py`**: The Uvicorn entry point.
- **`api.py`**: The FastAPI application definition and route handlers.

## Usage

See `docs/SERVER_GUIDE.md` for detailed instructions on how to run and configure this server.
